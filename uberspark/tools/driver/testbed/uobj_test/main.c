#include <uberspark.h>
#include <stdint.h>
#include <string.h>

char from_buffer[512];
char to_buffer[512];

void main(void){
 memset(buffer, 0, sizeof(buffer));
 memcpy(to_buffer, from_buffer, sizeof(to_buffer)); 
}
